package com.zdez.notes.adapter

import android.widget.TextView
import androidx.recyclerview.widget.RecyclerView

class TextItemViewHolder(view: TextView): RecyclerView.ViewHolder(view) {
    val bind: Unit
    val textView = view
}
